#####################################################################################
#
# calculate derivative
#
#####################################################################################

derivative <- function(x, y) {
  for (i in 1:(length(x)-1)) {
    x[[i]] <- x[[i+1]] - x[[i]]
    y[[i]] <- y[[i+1]] - y[[i]]
  }
  return(x/y)
}

